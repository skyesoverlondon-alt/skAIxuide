[build]
  publish = "."
  # functions not required for this static app, but leaving this aligned with Kaixu ecosystem defaults:
  functions = "netlify/functions"

[functions]
  node_bundler = "esbuild"

[[redirects]]
  from = "/api/*"
  to = "https://kaixugateway13.netlify.app/:splat"
  status = 200
  force = true
